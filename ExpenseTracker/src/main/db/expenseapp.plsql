CREATE TABLE "LOCATION" (	"LOCATION_ID" NUMBER NOT NULL ENABLE, "LOCATION" VARCHAR2(50) NOT NULL ENABLE, CONSTRAINT "LOCATION_PK" PRIMARY KEY ("LOCATION_ID") ENABLE, CONSTRAINT "LOCATION_UK1" UNIQUE ("LOCATION") ENABLE ) / CREATE TABLE "USER_CREDENTIAL" (	"ID" NUMBER NOT NULL ENABLE, "EMAIL" VARCHAR2(200) NOT NULL ENABLE, "PASSWORD" VARCHAR2(50) NOT NULL ENABLE, CONSTRAINT "USER_CREDENTIAL_ID_PK" PRIMARY KEY ("ID") ENABLE, CONSTRAINT "USER_CREDENTIAL_EMAIL_UK1" UNIQUE ("EMAIL") ENABLE ) / CREATE TABLE "USER_DETAILS" (	"USER_DETAILS_ID" NUMBER NOT NULL ENABLE, "FIRSTNAME" VARCHAR2(50), "LASTNAME" VARCHAR2(50), "GENDER" VARCHAR2(10), "LOCATION_ID" NUMBER, "ID" NUMBER, CONSTRAINT "USER_DETAILS_ID_PK" PRIMARY KEY ("USER_DETAILS_ID") ENABLE, CONSTRAINT "USER_DETAILS_LOCATION_FK" FOREIGN KEY ("LOCATION_ID") REFERENCES "LOCATION" ("LOCATION_ID") ENABLE, CONSTRAINT "USER_DETAILS_ID_FK2" FOREIGN KEY ("ID") REFERENCES "USER_CREDENTIAL" ("ID") ON DELETE CASCADE ENABLE ) / CREATE UNIQUE INDEX "USER_DETAILS_ID_PK" ON "USER_DETAILS" ("USER_DETAILS_ID") / CREATE UNIQUE INDEX "USER_CREDENTIAL_ID_PK" ON "USER_CREDENTIAL" ("ID") / CREATE UNIQUE INDEX "USER_CREDENTIAL_EMAIL_UK1" ON "USER_CREDENTIAL" ("EMAIL") / CREATE UNIQUE INDEX "LOCATION_UK1" ON "LOCATION" ("LOCATION") / CREATE UNIQUE INDEX "LOCATION_PK" ON "LOCATION" ("LOCATION_ID") / CREATE SEQUENCE "USER_DETAILS_ID_SEQ" MINVALUE 1 MAXVALUE 999999999999999999999999999 INCREMENT BY 1 START WITH 25 CACHE 20 NOORDER NOCYCLE / CREATE SEQUENCE "USER_CREDENTIAL_ID_SEQ" MINVALUE 1 MAXVALUE 999999999999999999999999999 INCREMENT BY 1 START WITH 29 CACHE 20 NOORDER NOCYCLE / CREATE SEQUENCE "LOCATION_SEQ" MINVALUE 1 MAXVALUE 999999999999999999999999999 INCREMENT BY 1 START WITH 206 CACHE 20 NOORDER NOCYCLE / CREATE OR REPLACE TRIGGER "BI_USER_DETAILS" before insert on "USER_DETAILS" for each row begin select "USER_DETAILS_ID_SEQ".nextval into :NEW.USER_DETAILS_ID from dual; end; / ALTER TRIGGER "BI_USER_DETAILS" ENABLE / CREATE OR REPLACE TRIGGER "BI_USER_CREDENTIAL" before insert on "USER_CREDENTIAL" for each row begin select "USER_CREDENTIAL_ID_SEQ".nextval into :NEW.ID from dual; end; / ALTER TRIGGER "BI_USER_CREDENTIAL" ENABLE / CREATE OR REPLACE TRIGGER "BI_LOCATION" before insert on "LOCATION" for each row begin select "LOCATION_SEQ".nextval into :NEW.LOCATION_ID from dual; end; / ALTER TRIGGER "BI_LOCATION" ENABLE /